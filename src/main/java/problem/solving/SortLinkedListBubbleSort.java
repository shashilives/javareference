package problem.solving;import java.util.Arrays;public class SortLinkedListBubbleSort {    public static void main(String[] args) {        SLL unsortedSLL = dummyList();        for (int i = 0; i < unsortedSLL.lenght; i++) {            Node prev = null;            Node curr = null;            Node next = null;            curr = unsortedSLL.head;            for (int j = 0; i < unsortedSLL.lenght - i - 2; j++) {                if (curr.value > curr.next.value) {                    next = curr.next;                    prev.next = next;                    next = curr;                }                prev = curr;                curr=curr.next;            }        }        System.out.println(unsortedSLL);    }    public static SLL dummyList() {        //4, 10, 1, 2, 3, 8        SLL sll = new SLL();        sll.head = new Node(4);        sll.head.next = new Node(10);        sll.head.next.next = new Node(1);        sll.head.next.next.next = new Node(2);        sll.head.next.next.next.next = new Node(3);        sll.tail = sll.head.next.next.next.next.next = new Node(8);        sll.lenght = 6;        return sll;    }////    private void prepareLinkedList(int[] a) {//        SLL sll = new SLL();//        Node node = null;//        for(int i = 0 ; i < a.length; i++){//            node//        }//    }    static class SLL {        Node head;        Node tail;        int lenght;    }    static class Node {        Node next;        int value;        public Node(int value) {            this.value = value;        }        @Override        public String toString() {            return "Node{" +                    "next=" + next +                    ", value=" + value +                    '}';        }    }}