package designpatterns.solid.o.old;import designpatterns.solid.o.Color;import designpatterns.solid.o.Product;import designpatterns.solid.o.Size;import java.util.List;import java.util.stream.Stream;public class ProductFilter {    //v1 was like this    public Stream filter(List<Product> productList, Color color) {        return productList.stream().filter(p -> p.color == color);    }    //v2 was like this    public Stream filter(List<Product> productList, Color color, Size size) {        return productList.stream().filter(p -> p.color == color && p.size == size);    }}