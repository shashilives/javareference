package datastructaures.trees;import java.util.ArrayList;import java.util.List;public class DSFInOrderTraversal {    public List<Node> dfsInOrder(Node root) {        List<Node> data = new ArrayList<>();        traverse(root, data);        return data;    }    private void traverse(Node node, List<Node> data) {        if (node.left != null) traverse(node.left, data);        data.add(node);        if (node.right != null) traverse(node.right, data);    }}